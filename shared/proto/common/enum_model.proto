syntax = "proto3";

package common;

option go_package = "shared/model";

enum OrderStatus {
  PENDING = 0;
  APPROVED = 1;
  SHIPPED = 2;
  DELIVERED = 3;
  CANCELLED = 4;
}

enum PaymentMethod {
  CREDIT_CARD = 0;
  DEBIT_CARD = 1;
  PAYPAL = 2;
  BANK_TRANSFER = 3;
}
